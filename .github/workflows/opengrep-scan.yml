name: OpenGrep Full Scan

on:
  workflow_dispatch:
  pull_request:   

jobs:
  scan:
    name: Scan with OpenGrep
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Install OpenGrep
        run: |
          # Download the OpenGrep binary
          wget https://github.com/opengrep/opengrep/releases/download/v1.0.0-alpha.9/opengrep_manylinux_x86 -O opengrep
          
          # Make the binary executable
          chmod +x opengrep

          # Move the binary to a directory in PATH
          sudo mv opengrep /usr/local/bin/opengrep

      - name: Run OpenGrep with custom rules
        run: |
          opengrep --metrics=off --error --include='*.php' --include='*.vue' --include='*.js' --include='*.ts' --exclude-rule='javascript.vue.security.audit.xss.templates.avoid-v-html.avoid-v-html' --sarif --sarif-output=findings.sarif --exclude='docs' .
          # If findings are detected (non-zero exit code), fail the step
          exit_code=$?
          if [ $exit_code -ne 0 ]; then
            echo "::error::OpenGrep found issues in the code. Check the report for details."
            exit 1
          fi

      - name: Show potential changes in OpenGrep results
        if: always()
        run: git diff --exit-code -- . ':!lib/composer'

      - name: Upload Analysis results to GitHub
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: findings.sarif